var app = app || {};
app.Project = Backbone.Model.extend({
    defaults: {
        p_category: 'Category 1',
        c_name: 'Project 1',
        c_description: 'Sample Project Description'
    }
});
